algoritmo "ITEM C"

var
soma, cont : inteiro

inicio

para cont de 1 ate 100 passo 1 faca
   soma <- cont + soma
   //escreval("A soma dos 100 primeiros números sequenciais é = ", soma)
fimpara
escreval("A soma dos 100 primeiros números sequenciais é = ", soma)


fimalgoritmo
